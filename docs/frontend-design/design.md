## refrence
- 原有的代码 [old](old)
- 主要参考网站设计，[网址](https://www.xinghuisama.top/),[源代码](XinghuisamaBlogs-main/my-blog-manager)

## 禁止
- 禁止使用emoji

## 测试账号
用户名：demotest
密码：TestPass123!

## 
首先我设计登录页和上方的导航栏, 以原有的登录页为基础，把导航栏在没登陆的时候只显示网址名字，还有一个白天黑夜模式的切换，完全参考设计网站的导航栏和切换白天黑夜的按钮的设计，动效要一致。

登录卡片使用液态玻璃，参考使用[源码](glass)

首页以我的旧代码为
上方的导航栏参考网站的导航栏，做一个有趣的鼠标聚焦效果在文件，注意登出的按钮也是用液态玻璃
下方的各种卡片页使用参考网站的设计，不要全一个尺寸

为我添加背景设计，黑衣使用萤火虫，白天使用樱花的动效，在网站里有
为我添加一个点击时有水波特效的点击效果，在网站的参考的代码中也有
接一下来设计搜索页，基于旧代码
重写一个符合当前风格的搜索栏，注意动效。
还有当前的卡片展示，只用改为毛玻璃即可，但是注意对于黑夜白天的改动的适应
最后的页面使用的底部栏，重构符合当前风格的，

我需要当前的搜索栏提供url参数的变化，我不希望每一次的下一页url都不变化，这也可以同时支持快捷的搜索跳转。页面跳转注意不要页面的全部的重刷新，注意性能

接下来开发藏书阁的首页，
展示的可以复用搜索页的，除了展示的状态栏不要显示`已下载`，全都改显示jm的id



优化，当前如果是白天模式，刷新是先显示黑夜模式才显示白天，这里有一个跳转的动画卡顿，应直接显示白天

还有本地资源
先提供两个链接，一个链接到本地的图片库，一个链接到视频库。
图片库采用多张图片堆叠的展示形式，在参考的网站中有，注意没有三张的时候，怎么处理，注意动效的复刻
视频库使用卡片的形式展示，就像藏书阁一样，注意有没有cover的图片，有就显示，没有就不显示


接下来设计搜索二级页面
首先设计一个加载页，有时候这个搜索页的漫画页的展示页会很慢，一定要精美，注意性能。
先设计搜索页的页面具体的漫画详情页设计，以原有的代码为基础，重构为液态玻璃的框，注意有时候这个album下的章节可以有很多，到几百章节，所以需要滑动或分页、
首先写一个demo给我看看

接下来设计藏书阁中的二级展示页
基于我的旧代码，风格和搜索页的展示页大致类似，
设计一个demo


优化，给当前的主题背景加草，在参考网站的源代码有，黑夜白天不同

接下来可以实现这个详情页的实现了，注意有很多可能是空的，比如简介，章节名。请考虑好空的情况


接下来设计视频的详情页，
我需要你参考B站的播放器，但不需要弹幕，不需要画质选择，
需要全屏，网页全屏，画中画，设置（二级，播完暂停or自动下一集），声音，进度条，播放时间等复刻，动效也要复刻。倍速也要复刻，注意我需要在手机端可以长按倍速，范围全屏长按倍速。
这个可以参考我的旧代码的,注意，这个可能有几百集考虑、
先做一个demo